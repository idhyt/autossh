[workspace]
members = ["atsh-lib", "atsh-cli"]
resolver = "2"
exclude = ["atsh-app/src-tauri"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 8
debug = false
strip = "symbols"
panic = "abort"
incremental = false
overflow-checks = false
